[package]
name = "dns-resolver-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
async-recursion = "1.0.2"
prometheus-exporter = "0.8"
lazy_static = "1.4.0"
